import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'Lends_Administration_App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
